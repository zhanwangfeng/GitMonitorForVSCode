# 变更日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.3] - 2026-08-18

### 优化工具栏图标

## [1.1.2] - 2026-08-18

### 优化svg图标

## [1.1.1] - 2026-08-18

### 新增图标支持

## [1.1.0] - 2026-08-17

### 首个可用版本（MVP）

#### 新增
- **侧边栏入口**：在活动栏注册 Git Monitor 图标，点击展开「仓库监控」视图
- **仓库列表展示**：以列表形式展示已添加的仓库条目，每个条目显示目录名、分支、领先/落后远端提交数、文件计数
- **状态图标**：根据仓库状态显示不同图标（干净 / 有修改 / 领先远端 / 落后远端 / 读取失败）
- **悬停提示**：鼠标悬停条目时以 Markdown 显示完整路径、远端地址、最新提交摘要
- **添加本地目录**：通过 `+` 按钮选择目录，自动校验是否为 Git 仓库，支持设置别名
- **详情面板**：点击条目打开 Webview 详情面板，分块展示
  - 基本信息（分支、上游、远端地址、状态徽章）
  - 同步状态（领先/落后提交数、工作区是否干净、文件计数汇总）
  - 文件变更（已暂存 / 已修改 / 未跟踪三组，附增删行数）
  - 最近提交（最近 20 条，含短 hash、作者、时间、消息）
  - 分支列表（本地 + 远程分支，当前分支高亮）
- **详情快捷操作**：刷新、在终端中打开、在资源管理器中打开、在新窗口打开、复制路径
- **移除监控**：从列表移除仓库（不影响本地文件），支持二次确认
- **刷新状态**：支持全部刷新（带并发上限）与单条刷新
- **自动轮询刷新**：可配置间隔自动刷新，VSCode 窗口失焦时自动暂停，恢复焦点时立即触发一次
- **配置项**：支持 7 项可配置参数（自动刷新、并发、超时、提交数、文件统计、git 路径等）
- **持久化**：仓库列表通过 globalState 持久化，跨工作区共享，重启 VSCode 后保留
- **启动探测**：激活时检测 git 是否可用，缺失时提示用户配置可执行文件路径
- **日志输出**：通过 `Git Monitor` 输出通道记录运行日志，时间格式为「年-月-日 时:分:秒」

#### 命令
- `gitMonitor.addRepo` — 添加目录
- `gitMonitor.refreshAll` — 全部刷新
- `gitMonitor.refreshRepo` — 刷新该仓库
- `gitMonitor.removeRepo` — 移除监控
- `gitMonitor.openDetail` — 打开详情
- `gitMonitor.openInExplorer` — 在资源管理器中打开
- `gitMonitor.openInTerminal` — 在终端中打开
- `gitMonitor.openInNewWindow` — 在新窗口中打开
- `gitMonitor.openSettings` — 打开设置

#### 菜单
- 视图标题栏：添加目录、全部刷新、打开设置
- 条目右键菜单：打开详情、刷新该仓库、在资源管理器中打开、在终端中打开、移除监控

#### 安全
- 仅读取 Git 元信息，不执行任何写操作（不 pull / push / commit）
- 使用 `execFile` 参数数组调用 git，避免 shell 注入与路径空格/中文问题